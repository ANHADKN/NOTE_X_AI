/* ==========================================
   NX-Apex Theme & Ambient Background Engine
   ========================================== */
(function() {
  // Ensure dark mode theme attribute is set
  document.documentElement.setAttribute('data-theme', 'dark');
})();
